// No need to export as all .gs files are loaded into the same namespace.
// Managed by `hug config` — run e.g.
//   hug config set SPREADSHEET_ID=<id> DRIVE_FOLDER_ID=<id>
const CONFIG = {
  SPREADSHEET_ID: '',
  DRIVE_FOLDER_ID: '',
};
